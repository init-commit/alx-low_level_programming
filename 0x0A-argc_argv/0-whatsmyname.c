#include <stdio.h>

/**
* main - prints program name
* @argc: argument count
* @argv: argument array
* Return: success
*/
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
